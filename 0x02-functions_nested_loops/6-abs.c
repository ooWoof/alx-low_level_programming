#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @a: The number to calculate the absolute value of int
 * Return: The absolute value of int
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
